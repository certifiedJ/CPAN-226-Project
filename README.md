To run it, you need to create .env file in project root with beloe info.

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

Replace the last 2 line with your own info, the password means Security > App Passwords (We created in lab3)
